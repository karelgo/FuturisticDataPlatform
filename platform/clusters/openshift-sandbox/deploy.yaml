# GENERATED — do not edit. Rendered from charts/carina for the Developer
# Sandbox (no helm needed there):
#   helm template carina charts/carina -f charts/carina/values-openshift.yaml \
#     --set image.repository=carina --set image.tag=0.7.0
# The bare image name resolves via the ImageStream lookupPolicy in build.yaml.
---
# Source: carina/templates/pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: carina-data
  labels:
    app.kubernetes.io/name: carina
    app.kubernetes.io/instance: carina
    app.kubernetes.io/version: "0.7.0"
    app.kubernetes.io/managed-by: Helm
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 2Gi
---
# Source: carina/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: carina
  labels:
    app.kubernetes.io/name: carina
    app.kubernetes.io/instance: carina
    app.kubernetes.io/version: "0.7.0"
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  ports:
    - port: 8899
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/name: carina
    app.kubernetes.io/instance: carina
---
# Source: carina/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: carina
  labels:
    app.kubernetes.io/name: carina
    app.kubernetes.io/instance: carina
    app.kubernetes.io/version: "0.7.0"
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: carina
      app.kubernetes.io/instance: carina
  template:
    metadata:
      labels:
        app.kubernetes.io/name: carina
        app.kubernetes.io/instance: carina
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: carina
          image: "carina:0.7.0"
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: true
          ports:
            - name: http
              containerPort: 8899
          env:
            - name: CARINA_ROOT
              value: /app
          readinessProbe:
            httpGet: {path: /api/lanes, port: http}
            initialDelaySeconds: 5
          livenessProbe:
            httpGet: {path: /api/lanes, port: http}
            initialDelaySeconds: 15
            periodSeconds: 30
          resources:
            limits:
              cpu: "2"
              memory: 2Gi
            requests:
              cpu: 250m
              memory: 512Mi
          volumeMounts:
            - name: data
              mountPath: /app/data
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: carina-data
        - name: tmp
          emptyDir: {}
---
# Source: carina/templates/route.yaml
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: carina
  labels:
    app.kubernetes.io/name: carina
    app.kubernetes.io/instance: carina
    app.kubernetes.io/version: "0.7.0"
    app.kubernetes.io/managed-by: Helm
spec:
  to:
    kind: Service
    name: carina
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
